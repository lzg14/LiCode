# Force git to treat these as text for proper diff/review
CHANGELOG.md text

# Normalize line endings to LF for all text files
* text=auto eol=lf

# Windows-specific files keep CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
